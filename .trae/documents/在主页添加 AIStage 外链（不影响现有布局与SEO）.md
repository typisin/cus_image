## 变更内容
- 在 `index.html` 的 `<footer>` 容器中添加外链：`<a href="https://aistage.net" title="AIStage" target="_blank" rel="noopener noreferrer">AIStage</a>`。
- 与版权信息同一行或分隔符连接，保持英文文案与页面风格一致。

## 位置与规范
- 文件：`/Users/youpengtu/Applications/cus_image/index.html`
- 区块：`<footer class="footer">` → `<div class="container">` 内的文案段
- 安全属性：`target="_blank"` + `rel="noopener noreferrer"`

## 验证
- 使用本地 Vercel 环境 `vercel dev` 预览首页，确认链接显示与跳转正常；不影响现有布局与 SEO。

我将按上述方案实施并立即完成验证。